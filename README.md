This application uses Spyne rpc framework with imput as HttpRPC and putput as JSON. 
Given the location parameters (lattitude and longitude) along with the radius, the application returns some useful data.

Check it out!
